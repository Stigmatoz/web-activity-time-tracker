export enum TypeOfList {
  Today,
  All,
  ByDays,
}

export enum SortingBy {
  UsageTime,
  Sessions,
}

export enum SettingsTab {
  GeneralSettings,
  WhiteList,
  Limits,
  Notifications,
  About,
}
